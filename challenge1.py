"""
Given a string of indeterminate length populated only with numerical digits, insert + or * between these digits to
return a mathematical equation that is equal to an inputed integer. If no solution is able to be found, return an
empty string.
"""

def getSol(numStr, strI, numSol):
    if strI == numStr.length:
        
def solution(numStr, numSol):
    return getSol(numStr, strI, numSol);
